- Git init  *membuat repo git master
- git add . *untuk semua file yang staging area
- git commit -m "pesan" *untuk commit
- git commit -am "pesan" *add sekaligus commit langsung"
- git log --all --decorate --oneline --graph *menampilkan graph
- alias graph="git log --all --decorate --oneline --graph" *membuat alias menampilkan graph
- git merge namabracnh (sebelum merge, pindah ke branch master terlebih dahulu)
- git merge --merge *untuk mengecek branch mana yang sudah di merge)
- git branch -d namabranch *menghapus branch
