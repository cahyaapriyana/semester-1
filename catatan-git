- Git init  *membuat repo git master
- git add . *untuk semua file yang staging area
- git commit -m "pesan" *untuk commit
- git commit -am "pesan" *add sekaligus commit langsung"
- git log --all --decorate --oneline --graph *menampilkan graph
- alias graph="git log --all --decorate --oneline --graph" *membuat alias menampilkan graph
- git merge namabracnh (sebelum merge, pindah ke branch master terlebih dahulu)
- git merge --merge *untuk mengecek branch mana yang sudah di merge)
- git branch -d namabranch *menghapus branch yang sudah di merge
- git branch -D namabranch *menghapus branch langsung 'tanpa cek udah dimerge atau belum !!!!'
- git status * mengecek status git
- git checkout *ganti branch
- git clone url * clone dari https
- git remote * mengecek remote (kalo bisa semuanya uptodate)
- git fetch *mengecek yang dilocal dan diremote, habis itu git status
- git pull *mengambil yang ada di remote
-- CATATAN : KALO TIDAK BISA DI PUSH, PULL TERLEBIH DAHULU, HABIS ITU GIT ADD, TERUS GIT COMMIT, TERUS GIT PUSH !!!---
- git remote -v

